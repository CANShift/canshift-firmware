[package]
name = "sensor-color-ramp"
version = "0.1.0"
edition = "2021"
publish = false
description = "Rust port of CANShift sensor color ramp logic — issue #1177 R-3 (firmware-only phase; wasm-bindgen + canshift-core integration lands in a follow-up)"
license = "UNLICENSED"

[lib]
crate-type = ["rlib", "staticlib"]

[features]
default = ["std"]
std = []
ffi = []
