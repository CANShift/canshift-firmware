{
  "name": "canshift-firmware",
  "version": "0.17.1",
  "scripts": {
    "prepare": "husky",
    "lint:pre-commit": "lint-staged"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.2.0"
  },
  "lint-staged": {
    "*.{cpp,h,hpp,inc}": "scripts/hooks/clang-format-staged.sh",
    "*.{md,yml,yaml,json}": "npx prettier --write --ignore-unknown"
  }
}
